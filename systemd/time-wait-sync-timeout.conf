# Drop-in for systemd-time-wait-sync.service (installed by systemd/install.sh).
# The stock unit waits forever; with no network the hexapod stack would never start.
# After 90 s the unit fails, time-sync.target is still reached, and the stack starts on
# the restored clock (DEC-21).
[Service]
TimeoutStartSec=90
