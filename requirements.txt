# Python packages not available from Ubuntu 24.04 apt, installed into the
# system interpreter (ROS 2 nodes run under /usr/bin/python3):
#   pip3 install --break-system-packages -r requirements.txt
#
# Everything else comes from apt (see scripts/ubuntu-setup.sh):
#   python3-gpiozero python3-lgpio python3-spidev python3-smbus
#   python3-numpy python3-opencv python3-flask
rpi-ws281x>=5.0.0
mpu6050-raspberrypi>=1.2
face_recognition>=1.3   # builds dlib from source, ~20 min on Pi 5
